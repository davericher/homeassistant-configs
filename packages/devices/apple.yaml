# Home Kit
homekit:
  filter:
    include_domains:
      - light
      - media_player
device_tracker:
  # ICloud Account
  - platform: icloud
    username: !secret icloud_username
    password: !secret icloud_password
    account_name: Daves ICloud Account
